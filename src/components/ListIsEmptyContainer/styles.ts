import { StyleSheet } from "react-native";

export const styles = (props: any) =>
  StyleSheet.create({
    main: {
      flex: 1,
      justifyContent: "center",
      alignItems: "center",
      marginTop: "50%",
    },
  });
