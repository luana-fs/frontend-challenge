import { getAllUsers } from "../../src/services/Users"

describe('contexts/UsersContext', () => {
    describe('gettAllUsers', () => {
        if ('deve retornar uma lista de usuários', async () => {
             const users = await getAllUsers()
         })
     })
 })